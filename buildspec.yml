version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
  pre_build:
    commands
      - echo Nothing to do (maybe run test) in the pre_build phase... 
  build:
    commands:
      - echo Build started on `date`
      - npm run build
  post_build:
    commands:
      - echo Build (maybe zip build dir) completed on `date`
artifacts:
  nodejs_aartifacts:
    base-directory: .
    files:
      - build/
